VITE_API_URL=/api/v1
# Optional. Enables the WalletConnect option in the wallet picker (mobile and
# QR wallets). Create a free project id at https://cloud.reown.com. Injected
# wallets such as MetaMask work without it.
VITE_WALLETCONNECT_PROJECT_ID=
